package com.galaxy.leetcode.demo;

import sun.misc.Unsafe;

import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.atomic.AtomicInteger;

public class MeiTuanDemo {
//    List<Server> servers;
//
//    AtomicInteger pos = 0;
//    private static final Unsafe unsafe = Unsafe.getUnsafe();
//
//    public Server select() {
//        Object server = null;
//        int temp = pos.incrementAndGet() % servers.size();
//        if(temp > servers.size() + 1000){
//            unsafe.getAndSetInt(pos, temp, 0);
//        }
//        server = servers.get(temp);
//        return server;
//    }
}
